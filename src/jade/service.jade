extends inc/temp

append var
    - var thisPage = 'Our Service'
    - var pageSubhead = 'What we doing'
    - var pageTitle = pageTitle.concat(' | ' + thisPage)

append main
    include inc/hosting-services
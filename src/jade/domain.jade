extends inc/temp

append var
    - var thisPage = 'Domain'
    - var pageSubhead = 'Registration'
    - var pageTitle = pageTitle.concat(' | ' + thisPage)
    - var selected = 'domain'

append main
    section
        include inc/register-domain
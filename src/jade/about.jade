extends inc/temp

append var
    - var thisPage = 'About us'
    - var pageSubhead = 'What we are'
    - var pageTitle = pageTitle.concat(' | ' + thisPage)
    - var selected = 'about us'

append main
    include inc/details
    include inc/offers
    include inc/team